<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/bootstrap.css' />
    <link rel='stylesheet' href='/stylesheets/bootstrap.min.css' />
    <link rel='stylesheet' href='/stylesheets/bootstrap-responsive.min.css' />
    <link rel='stylesheet' href='/stylesheets/bootstrap-responsive.css' />
    <% scripts.push('jquery-1.9.1.min.js', 'jquery.validate.js', 'signupform.js', 'bootstrap.js', 'googlemaps.js?v=3.exp&sensor=false', 'map.js') %>
    <%- renderScriptsTags(scripts) %>
  </head>
  <body>
    <div class="span3"></div>
    <div class="span6">
      <div id="legend">
        <legend class="">Event: <%= even.fullname %></legend>
      </div>

      <b>Event information:</b>
      <p><%= even.descr %></p>

      <p><b>Start date:</b> <%= even.sdate.getUTCFullYear() %>-<%= ('0' + (even.sdate.getUTCMonth() + 1)).slice(-2)%>-<%= ('0' + even.sdate.getUTCDate()).slice(-2) %> <%= ('0' + even.sdate.getHours()).slice(-2) %>:<%= ('0' + even.sdate.getMinutes()).slice(-2) %></p>

      <p></p><b>End date:</b> <%= even.edate.getUTCFullYear() %>-<%= ('0' + (even.edate.getUTCMonth() + 1)).slice(-2) %>-<%= ('0' + even.edate.getUTCDate()).slice(-2) %> <%= ('0' + even.edate.getHours()).slice(-2) %>:<%= ('0' + even.edate.getMinutes()).slice(-2) %></p>

      <b>Location:</b>
      <input type="hidden" id="address" value="<%=even.location %>">
      <div id="map-canvas" style="height:50%; width=50%;"></div>
      <div class="btn-toolbar">
      <td class="span2">
        <a class="btn btn-primary" href="/app/settings/<%=even.ename %>"><i class="icon-wrench icon-white"></i> App settings</a>
        <a class="btn btn-success" href="/app/build/<%=even.ename %>"><i class="icon-qrcode icon-white"></i> Build app</a>
      </td>
      </div>
    </div>
  </body>
</html>